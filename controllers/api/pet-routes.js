const router = require('express').Router();
// TODO: include models here and change directory path if necessary >>> const { Pet } = require('../models');

// TODO: POST a new pet

module.exports = router;